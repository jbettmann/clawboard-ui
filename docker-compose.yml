services:
  clawboard-ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: clawboard-ui:latest
    container_name: clawboard-ui
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
